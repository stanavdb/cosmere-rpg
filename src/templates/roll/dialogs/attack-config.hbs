{{log this}}
<form>
    <div class="form-group">
        <label>{{ localize "GENERIC.Formula"}}</label>
        <input type="text" name="formula" value="{{skillTest.formula}}" disabled>
    </div>

    <div class="form-group-stacked">
        <h5>{{localize "DIALOG.ConfigureAttack.AdvantageMode.Title"}}</h5>
        <ul class="dice-config">
            <li class="dice header">
                <div class="col dice">
                    <span>{{localize "DIALOG.ConfigureAttack.AdvantageMode.Field.Dice"}}</span>
                </div>
                <div class="col type">
                    <span>{{localize "DIALOG.ConfigureAttack.AdvantageMode.Field.Type"}}</span>
                </div>
                <div class="col mode"></div>
            </li>

            {{!-- Skill test --}}
            <li class="dice">
                <div class="col dice">
                    <span>{{skillTest.dice}}</span>
                </div>
                <div class="col type">
                    <span>{{localize "GENERIC.SkillTest"}}</span>
                </div>
                <div class="col mode">
                    {{app-multi-state-toggle
                        name="skillTest.advantageMode"
                        selected=skillTest.advantageMode
                        states=advantageModes
                    }}
                </div>
            </li>

            {{!-- Plot die --}}
            {{#if skillTest.plotDie}}
            <li class="dice">
                <div class="col dice">
                    <span>1dp</span>
                </div>
                <div class="col type">
                    <span>{{localize "GENERIC.Plot"}}</span>
                </div>
                <div class="col mode">
                    {{app-multi-state-toggle
                        name="skillTest.advantageModePlot"
                        selected=skillTest.advantageModePlot
                        states=advantageModes
                    }}
                </div>
            </li>
            {{/if}}

            {{!-- Damage roll --}}
            <li class="dice">
                <div class="col dice">
                    <span>{{damageRoll.dice}}</span>
                </div>
                <div class="col type">
                    <span>{{localize "GENERIC.Damage"}}</span>
                </div>
                <div class="col mode">
                    {{app-multi-state-toggle
                        name="damageRoll.advantageMode"
                        selected=damageRoll.advantageMode
                        states=advantageModes
                    }}
                </div>
            </li>
        </ul>
    </div>

    <div class="form-group">
        <label>{{ localize 'COSMERE.Actor.Attribute.name' }}</label>
        <select name="attribute">
            {{selectOptions attributes selected=defaultAttribute localize=true}}
        </select>
    </div>

    <div class="form-group">
        <label>{{ localize "DICE.Plot.RaiseTheStakes" }}</label>
        <input type="checkbox" name="plotDie" {{#if skillTest.plotDie}}checked{{/if}}>
    </div>

    <div class="form-group">
        <label>{{ localize "DICE.RollMode" }}</label>
        <select name="rollMode">
            {{selectOptions rollModes selected=defaultRollMode localize=true}}
        </select>
    </div>

    <div class="form-group">
        <label>{{ localize "DICE.TemporaryBonus" }}</label>
        <input name="temporaryMod" type="text" value="{{skillTest.temporaryModifiers}}"/>
    </div>

    <br>
    <div class="form-group">
        <button data-action="submit" 
            type="submit"
        >
            {{ localize "GENERIC.Button.Roll" }}
        </button>
    </div>
</form>