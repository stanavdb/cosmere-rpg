<section class="sheet-content flexrow">
    <section class="col-left">
        <app-actor-details></app-actor-details>
        {{#each resources as |resourceId|}}
        {{{component 'app-actor-resource' resource=resourceId}}}
        {{/each}}
        <app-character-favorites class="list-component"></app-character-favorites>
    </section>
    <section class="col-main">
        <app-actor-attributes></app-actor-attributes>
        <div class="tab-body">
            {{> char-details-tab}}
            {{> char-actions-tab}}
            {{> char-equipment-tab}}
            {{> char-goals-tab}}
            {{> char-effects-tab}}
        </div>
    </section>
</section>